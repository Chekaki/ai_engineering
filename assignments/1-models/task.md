# Домашнє завдання №1: Foundational Models & Sampling Parameters

## Базовий код у `starter.py` — копіюй, змінюй `CASE_NAME` і `PROMPT` для кожного кейсу

## Мета завдання

Навчитися на практиці керувати поведінкою моделей за допомогою параметрів семплювання (`Temperature`, `Top-P`) та порівняти можливості великих хмарних моделей з локальними маленькими моделями.

---

## 1. Foundational Models

Створити таблицю з принаймні 10 models (5 large, 5 small, proprietary чи open-weights) які хочеш спробувати у своїй роботі. Коротко зазнач чому саме ця модель.

**Де шукати моделі та параметри:**
- [openrouter.ai/models](https://openrouter.ai/models) — агрегатор з цінами, context window, модальностями та архітектурою (найзручніше)
- [huggingface.co/models](https://huggingface.co/models) — model cards з технічними деталями (vocab size, архітектура)
- [artificialanalysis.ai](https://artificialanalysis.ai) — порівняння швидкості та ціни
- [openai.com/api/pricing](https://openai.com/api/pricing), [anthropic.com/pricing](https://anthropic.com/pricing) — офіційні ціни

Для кожної моделі зібрати ключові інженерні параметри:

- Назва моделі та провайдер
- Тип (Proprietary / Open-weights)
- Context Window (розмір контекстного вікна)
- Ціна (Input/Output за 1M токенів)
- Модальності (Text, Vision, Audio, Video)
- **Vocab size** (к-сть токенів у словнику: 32K? 128K? 256K?) — шукай у model card на HuggingFace або офіційній документації; для OpenAI моделей — через [tiktoken](https://github.com/openai/tiktoken)
- **Architecture** (Dense / MoE та кількість шарів, якщо відомо) — зазвичай є в blog post або технічному звіті моделі

## 2. Embedding Models

Знайти та виписати параметри для 5 популярних моделей ембедінгів:

- Dimensions: Довжина вектора (1536, 1024?)
- Max Input Tokens: Обсяг тексту для одного вектора
- MTEB Score: Місце в Massive Text Embedding Benchmark
- Ціна/Хостинг: Вартість або можливість локального запуску

Приклади моделей: `text-embedding-3-small`, `multilingual-e5-large`, `cohere-embed-v3`

---

## 🛠 Інструментарій

* **Large Model (хмара):** будь-яка потужна модель через [OpenRouter](https://openrouter.ai/) — наприклад, `gpt-4o`, `claude-sonnet`, `llama-4-scout`.
* **Small / Local Model:** обери будь-яку модель до ~8B параметрів — через OpenRouter або локально через Ollama. Головне: вона має бути помітно менша за твою Large model.
  - Рекомендовані варіанти: `google/gemma-3-4b-it`, `meta-llama/llama-3.2-3b-instruct`, `phi-4-mini`, `qwen3-8b` — або будь-яка інша з [openrouter.ai/models](https://openrouter.ai/models) у ціновому діапазоні $0.01–0.10/M токенів
  - Локально через Ollama: `ollama pull gemma3:2b` або будь-яка інша підтримувана модель з [ollama.com/library](https://ollama.com/library)

Як налаштувати — дивись `router_setup.md`.

---

## Опис експерименту

Для кожного з **кейсів 1–4** виконайте **6 запусків** через `starter.py`:
1. Large Model при T=0.1, 0.7, 1.2.
2. Small Model при T=0.1, 0.7, 1.2.

**Кейс 5 (Capability Ladder)** — окремий скрипт: `uv run ladder.py`

Разом: 4 кейси × 6 запусків + 6 запусків ladder = **30 автоматичних запусків**.

Кожен запуск показує **час (секунди)** та **вартість ($)** прямо в терміналі.

Перед кожним запуском `starter.py` зміни:
- `CASE_NAME` → назву поточного кейсу (наприклад, `"case1_data_extraction"`)
- `SYSTEM_PROMPT` → інструкція для моделі: що робити, яким має бути формат відповіді
- `PROMPT` → контекст або запитання: текст логу, правила, статтю — те над чим модель працює

Приклад для Кейсу 1:
```python
SYSTEM_PROMPT = "Extract the following fields into a valid JSON array (one object per log line): timestamp, status_code, request_time_ms, error_type, client_ip. If a field is missing, use null. Skip non-log lines like [SYSTEM], [USER_NOTE], [DEBUG]."

PROMPT = """[SYSTEM] Session started at 2026-02-27T14:10:00Z
2026-02-27 14:10:05.123 INFO  192.168.1.15 GET /api/v1/user/profile 200 45ms
...(весь лог)
"""
```

**Загальні налаштування для всіх тестів:**
* `Top-P`: **0.9** (фіксовано).
* `Seed`: **42** (для відтворюваності).

Результати зберігаються у `responses/<CASE_NAME>/`.

---

## 📂 Кейси для тестування

### Кейс 1: Data Extraction (Тест на точність)

**Завдання:** Витягни дані з лога у форматі JSON.  
**Очікувані поля:** `timestamp`, `status_code`, `request_time_ms`, `error_type`, `client_ip`.

**Промпт:**
> "Extract the following fields into a valid JSON: timestamp, status_code, request_time_ms, error_type, client_ip. If a field is missing, use null."

**Контекст (Лог):**
```text
[SYSTEM] Session started at 2026-02-27T14:10:00Z
2026-02-27 14:10:05.123 INFO  192.168.1.15 GET /api/v1/user/profile 200 45ms
--- gap in logs ---
1740665406 WARN  192.168.1.15 GET /api/v1/auth/login 401 12ms (invalid_token)
1740665408 ERROR 10.0.5.102 POST /api/v1/orders/create 500 1500ms (db_timeout)
[USER_NOTE]: I think the server is lagging here.
2026-02-27 14:10:12.445 DEBUG 172.16.0.44 GET /health 200 2ms
2026-02-27 14:10:15.901 FATAL 192.168.1.88 POST /api/v2/pay 499 30000ms (timeout)
2026-02-27 14:10:16.001 INFO  UNKNOWN_IP GET /static/logo.png 200 5ms
2026-02-27 14:10:17.000 ERROR 8.8.8.8 CONNECT google.com:443 "CONNECTION_REFUSED" 0ms
[DEBUG] Ignoring this internal trace: 0x0045FF2
2026-02-27 14:10:18.111 INFO  127.0.0.1 GET /admin 403 10ms
1740665420 WARN  192.168.1.15 GET /api/v1/search?q=AI_Engineering 200 450ms
2026-02-27 14:10:22.000 INFO 1.1.1.1 GET /favicon.ico 200 1ms
ERROR: [CRITICAL_SYSTEM_FAILURE] - NO_IP_FOUND - 505 - 0ms - (internal_kernel_panic)
2026-02-27 14:10:25.123 INFO 192.168.1.15 GET /api/v1/user/settings 200 38ms
2026-02-27 14:10:26.000 WARN 10.0.0.1 DELETE /account 401 5ms (session_expired)
[INFO] End of batch. Total processed: 12 lines.
```

**На що дивитися?**

1. **Галюцинація UNIX timestamp:** Рядки з `1740665406` — це Unix timestamp. Чи залишила модель його як є (правильно), чи придумала дату у форматі `"2026-02-27 14:10:06.123"`? Маленькі моделі часто галюцинують тут.
2. **Log level vs error_type:** Рядок `WARN ... (invalid_token)` — `error_type` має бути `"invalid_token"`, а не `"WARN"`. Маленька модель може переплутати лог-рівень і тип помилки.
3. **Статус 499:** Нестандартний код. Чи не «виправила» модель його на 404 або 500?
4. **Ігнорування шуму:** Чи не розпарсила модель рядок `[USER_NOTE]` або `[SYSTEM]` як частину JSON?
5. **Small vs Large:** Порівняйте кількість записів та точність полів. Де саме маленька модель починає вигадувати?

---

### Кейс 2: Text Summarization (Стислий підсумок)

**Завдання:** Зроби стислий технічний підсумок тексту у форматі 5 bullet points українською мовою.

**Промпт:**
> Зроби стислий технічний підсумок тексту у форматі 5 bullet points українською мовою. Зосередься на причинах виникнення проблеми та методах її вирішення.

**Контекст (Текст про галюцинації):**
```text
Галюцинації в сучасних мовних моделях (LLM) виникають через те, що вони за своєю природою є статистичними предикторами наступного токена, 
а не логічними базами даних. Модель не "знає" фактів, вона лише обчислює найімовірнішу послідовність символів. 
Основною причиною помилок є застарілі дані (data cut-off) та відсутність механізму перевірки тверджень у реальному часі. 
Для мінімізації цього ефекту в AI Engineering використовують архітектуру RAG (Retrieval-Augmented Generation), 
яка "приземлює" відповіді моделі на конкретні документи. 
Також важливо контролювати параметр Temperature: при значеннях понад 1.2 модель починає надавати надмірну вагу малоймовірним токенам, 
що призводить до вигадування неіснуючих фактів. 
Дослідження показують, що впровадження RAG разом із системними промптами на кшталт "відповідай лише на основі тексту" 
знижує рівень галюцинацій на 60-70% у порівнянні зі звичайним чатом.
```

**На що дивитися?**

1. **Точність цифр:** У тексті вказано зниження галюцинацій на 60-70%. При T=1.2 маленькі моделі можуть змінити цифру або сформулювати розмито ("суттєво знижує" замість конкретного відсотка). Велика модель тримає факти навіть при T=1.2.
2. **Зайве форматування:** Чи додала маленька модель заголовки (`**Причина:**`) яких не просили? Це ознака того що модель "намагається" але не чує точні інструкції.
3. **Якість і зв'язність:** Порівняйте довжину речень і логічний зв'язок між bullet points у малої vs великої моделі. Де очевидна різниця в "розумінні"?

---

### Кейс 3: Складний Q&A (Reasoning & Extraction)

**Завдання:** Обчисли підсумкову вартість та визнач статус акаунта на основі заданих умов.

**Промпт:**
> Дай відповідь на основі тексту:
>
> Якою буде вартість зберігання 120 ГБ даних для акаунта типу 'Academic', якщо він обрав річну підписку?
>
> Чи буде такий акаунт заблоковано, якщо він завантажить 5 терабайт даних за один день?
>
> Відповідь має бути короткою, з посиланням на конкретні пункти правил.

**Контекст (Правила хмарного сховища 'Nebula-V'):**
```text
1. Типи акаунтів:
Користувачі 'Standard' мають ліміт 10 ГБ. Акаунти 'Academic' (верифікація через .edu) отримують 50 ГБ безкоштовно.

2. Тарифікація понад ліміт:
Кожен додатковий ГБ коштує $0.50/міс. При виборі річної підписки діє знижка 20% на весь обсяг додаткового зберігання. ВАЖЛИВО: Для акаунтів 'Academic' знижка на річну підписку становить лише 10%, але вона додається до базової студентської знижки у $2 на загальний рахунок.

3. Політика безпеки (Fair Use):
Денний ліміт завантаження (Ingress) становить 1 ТБ для всіх типів акаунтів. Якщо обсяг завантаження перевищує ліміт у 3 рази протягом доби, акаунт переходить у статус 'Pending'. Якщо перевищення складає понад 5 разів — акаунт блокується негайно без права апеляції.

4. Спеціальні умови:
Студенти (Academic) можуть подати запит на збільшення ліміту Ingress до 10 ТБ, але ця опція активується лише через 48 годин після верифікації карти. До цього моменту діють загальні правила безпеки (п. 3).
```

**На що дивитися?**

1. **Математичний ланцюжок:** Правильна відповідь — **$29.50/міс**. Більшість моделей помиляються: плутають місячну та річну суму, або трактують $2 як одноразову знижку замість щомісячної. Спробуй різні температури — цікаво, при якій модель знайде правильний ланцюжок.
2. **Критична пастка — поріг блокування:** 5 ТБ / 1 ТБ = **рівно 5 разів** → негайне блокування (не 'Pending'!). Маленька модель може написати "перевищує 3 рази" — це неправильний поріг, і акаунт отримає статус 'Pending' замість блокування.
3. **Small vs Large:** Чи маленька модель дочитала до п. 4 (спецумови про 48 годин)? Чи взагалі врахувала, що знижка 10% застосовується ТІЛЬКИ до додаткового об'єму, а не до всього?
4. **Temperature effect on reasoning:** Для якої температури кожна модель дала найближчу відповідь до правильної? Чи низька температура завжди краща для reasoning?

---

### Кейс 4: Creative Pitch (Тест на варіативність)

**Завдання:** Напиши короткий рекламний опис (3-4 речення) для CLI-інструменту.

**Промпт:**
> "Write a catchy 3-sentence marketing pitch for a developer tool named 'Git-Burn'. Its main function is to automatically delete git branches that haven't been touched for 30 days and send a Slack notification to the last committer. Target audience: lazy DevOps engineers."

**На що дивитися?**

1. **Ліміт 3 речення:** Велика модель тримає рівно 3 речення при всіх температурах. Маленька модель може видати "Here are a few options..." і написати 3 варіанти замість одного — класичне порушення інструкції.
2. **Словниковий запас (Large):** Порівняйте T=0.1 ("cleanup crew", "automated purge") та T=1.2 ("annihilate", "pristine", "secret weapon"). Зміна очевидна.
3. **Small vs Large на instruction following:** Де більша різниця — у якості тексту чи у дотриманні формату? Це показує різні типи gap між моделями.
4. **Top-P Bonus:** При Top-P = 0.1 текст буде максимально банальним, майже як у робота 80-х.

---

### Кейс 5: Capability Ladder (Сходи можливостей)

**Завдання:** Запусти `ladder.py` — він пройде ту саму задачу (Nebula-V, Кейс 3) через 6 моделей різного розміру при T=0.1 і покаже як змінюється якість, час та вартість із зростанням параметрів.

```bash
uv run ladder.py
```

Моделі (всі 2024-2025):

| Tier | Модель | Архітектура | Active params | Total params | Рік |
|---|---|---|---|---|---|
| Nano | llama-3.2-1b | Dense | 1B | 1B | 2024 |
| Small | gemma-3-4b-it | Dense | 4B | 4B | 2025 |
| **MoE lite** | **qwen3-30b-a3b** | **MoE** | **3B** | **30B** | **2026** |
| **MoE heavy** | **llama-4-scout** | **MoE** | **17B** | **109B** | **2025** |
| Large | llama-3.3-70b | Dense | 70B | 70B | 2024 |
| Frontier | gpt-4o | ? | ? | ? | 2024 |

> 💡 **MoE (Mixture of Experts):** Для кожного токена модель активує лише частину параметрів — "experts". Qwen3-30B: 3B активних з 30B (коштує як 3B!). LLaMA 4 Scout: 17B активних з 109B (коштує як 17B!). Це та сама архітектура яку ми розглядали в лекції. Порівняй: яка MoE модель кращий трейдоф?

**На що дивитися?**

1. **Перший правильний math:** При якому розмірі модель найближча до правильної відповіді ($29.50/міс)? (Підказка: відповідь може здивувати — не обов'язково найбільша)
2. **Поріг блокування:** При якому розмірі модель правильно розпізнає що 5 ТБ = 5× = негайний блок (а не 'Pending')? При якому плутає поріг?
3. **MoE marketing vs реальність:** Qwen3 каже "3B active" — дешевий як 3B? Перевір реальну ціну і час. Потім порівняй Scout (17B active) — чи виправдовує Scout своє "більше активних параметрів" кращим результатом і ціною? А тепер обох з Dense 70B.
4. **Capability is NOT monotonic:** Чи дійсно більша модель (за active params) дала кращий результат? Якщо ні — як це пояснити?
5. **Speed + Cost:** Яка модель найдешевша і найшвидша серед тих що дали прийнятну відповідь?

**Рефлексія:** Якби ти будував продукт з 1,000 запитів на день на цій задачі — який tier обрав би і чому?

---

## Бонус A: The Nucleus Effect

Візьміть **Кейс 4** (Creative Pitch), зафіксуйте T=1.0 та порівняйте два запуски:
- Top-P = 1.0
- Top-P = 0.1

**Питання:** Як обмеження "ядра" ймовірностей (Top-P) вплинуло на творчість моделі?  
Опишіть різницю одним-двома реченнями.

---

## Бонус B: System Prompt Experiment

Візьміть **Кейс 4** (Creative Pitch). Запустіть `google/gemma-3-4b-it` при T=0.7 **двічі**:

1. Без system prompt (результат вже є з Кейсу 4)
2. В `starter.py` знайди функцію `ask()` і додай system message перед user message:
   ```python
   # Було:
   messages=[{"role": "user", "content": prompt}],
   # Стало:
   messages=[
       {"role": "system", "content": "You must write exactly 3 sentences. Do not provide multiple options or variants."},
       {"role": "user", "content": prompt},
   ],
   ```

**Питання:** Чи виправив system prompt проблему з форматом ("Here are a few options...")?  
Що це говорить про різницю між "модель не вміє" і "модель не знала що від неї хочуть"?
